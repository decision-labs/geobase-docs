import { Callout } from 'nextra/components'

# GeoEmbeddings + GeoParquet (placeholder)

<Callout type="warning" emoji="⏳">
  This page is a temporary placeholder. The full “Connect to GeoParquet” guide is coming soon.
</Callout>

This placeholder covers the GeoEmbeddings artifacts flow in Studio:

- Worker uploads GeoParquet artifacts to Supabase Storage
- Studio generates time-limited signed HTTPS URLs
- “Connect to GeoParquet” provides ready-to-run snippets (curl, Python, DuckDB)

